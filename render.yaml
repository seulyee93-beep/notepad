services:
  # Backend API (Web Service - Starter)
  - type: web
    name: notepad-api
    runtime: node
    plan: starter
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: notepad-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://notepad-app.onrender.com

  # Frontend (Static Site - Starter)
  - type: web
    env: static
    name: notepad-app
    plan: starter
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://notepad-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  # PostgreSQL (Starter)
  - name: notepad-db
    plan: starter
    databaseName: notepad
    user: notepad
